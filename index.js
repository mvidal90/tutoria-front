
let activeSection = "Historic";

const selectSection = (section) => {
    activeSection = section;

    switch (activeSection) {
        case "Historic":
            
            break;

        case "Filters":
            
            break;
        case "Panel":
            
            break;
    
        default:
            
            break;
    }
}